const fs=require('fs'),path=require('path'),{spawnSync}=require('child_process');
const root=path.join(__dirname,'..');const files=[];function walk(d){for(const e of fs.readdirSync(d,{withFileTypes:true})){const p=path.join(d,e.name);if(e.isDirectory()&&!['node_modules','dist'].includes(e.name))walk(p);else if(e.isFile()&&/\.(c?js)$/.test(e.name)&&!p.endsWith('check.cjs'))files.push(p)}}walk(root);for(const f of files){const r=spawnSync(process.execPath,['--check',f],{encoding:'utf8'});if(r.status!==0){console.error(r.stderr||r.stdout);process.exit(r.status||1)}}console.log(`Syntax OK: ${files.length} JavaScript-Dateien`);
