class YouTubeAdapter{
  constructor({liveChatId='',apiKey='',pollMs=2500,onMessage,onStatus}){this.name='youtube';this.liveChatId=liveChatId;this.apiKey=apiKey;this.pollMs=pollMs;this.onMessage=onMessage;this.onStatus=onStatus;this.running=false;this.timer=null;this.pageToken='';this.backoff=pollMs;this.status={name:this.name,connected:false,state:'not-configured',error:null}}
  getStatus(){return{...this.status}}setStatus(p){this.status={...this.status,...p};this.onStatus?.(this.getStatus())}updateConfig(c={}){this.liveChatId=c.liveChatId??this.liveChatId;this.apiKey=c.apiKey??this.apiKey;this.pollMs=Math.max(1000,Number(c.pollMs||this.pollMs||2500));this.setStatus({state:this.liveChatId&&this.apiKey?'configured':'not-configured'})}
  async connect(){if(!this.liveChatId||!this.apiKey)throw new Error('YouTube benötigt Live Chat ID und API Key.');if(this.running)return{ok:true,status:this.getStatus()};this.running=true;this.backoff=this.pollMs;this.setStatus({state:'connecting',connected:false,error:null});await this.poll();return{ok:true,status:this.getStatus()}}
  async poll(){if(!this.running)return;try{const qs=new URLSearchParams({liveChatId:this.liveChatId,part:'snippet,authorDetails',maxResults:'200',key:this.apiKey});if(this.pageToken)qs.set('pageToken',this.pageToken);const r=await fetch(`https://www.googleapis.com/youtube/v3/liveChat/messages?${qs}`);if(!r.ok)throw new Error(`YouTube API ${r.status}`);const j=await r.json();for(const item of j.items||[]){const s=item.snippet||{},a=item.authorDetails||{};this.onMessage?.({platform:'youtube',id:item.id,userId:a.channelId,username:a.displayName||'YouTubeUser',displayName:a.displayName,message:s.displayMessage||'',timestamp:s.publishedAt||new Date().toISOString(),moderator:Boolean(a.isChatModerator||a.isChatOwner),subscriber:Boolean(a.isChatSponsor),raw:item})}this.pageToken=j.nextPageToken||this.pageToken;this.backoff=Math.max(this.pollMs,Number(j.pollingIntervalMillis||this.pollMs));this.setStatus({state:'connected',connected:true,error:null});}catch(e){this.backoff=Math.min(60000,Math.max(5000,this.backoff*2));this.setStatus({state:'error',connected:false,error:e.message})}finally{if(this.running)this.timer=setTimeout(()=>this.poll(),this.backoff)}}
  disconnect(){this.running=false;clearTimeout(this.timer);this.timer=null;this.setStatus({state:'stopped',connected:false,error:null})}
}
module.exports={YouTubeAdapter};
